services:
  - type: static
    name: biomic-whiz-landing
    buildCommand: echo "Static landing page"
    staticPublishPath: static-site
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
